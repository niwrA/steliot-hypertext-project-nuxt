import { describe,expect,it } from 'vitest';import page from '../public/content/pages/burbank-notes-burbank-a.json';import { renderLegacy } from '../app/utils/content'
describe('server content renderer',()=>{it('rewrites annotation links to ordinary URLs',()=>{const html=renderLegacy(page as never);expect(html).toContain('href="/annotation/burbank-notes-title-n/"');expect(html).not.toContain('href="title_N.htm"')});it('rewrites assets to canonical root paths',()=>{expect(renderLegacy(page as never)).toContain('/content/assets/')})})
